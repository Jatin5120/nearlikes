import 'package:flutter/material.dart';
import 'constants.dart';

EdgeInsets pagePadding(Size size) => EdgeInsets.symmetric(
      horizontal: size.width.tenPercent,
      vertical: size.height.fivePercent,
    );
